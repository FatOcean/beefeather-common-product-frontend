

# 文档解析系统接口文档

## 目录

- [1. 任务管理接口](#1-任务管理接口)
  - [1.1 获取任务列表](#11-获取任务列表)
  - [1.2 创建任务](#12-创建任务)
  - [1.3 删除任务](#13-删除任务)
- [2. 单据类型接口](#2-单据类型接口)
  - [2.1 获取单据类型列表](#21-获取单据类型列表)
- [3. 文件管理接口](#3-文件管理接口)
  - [3.1 上传文件](#31-上传文件)
- [4. 分类管理接口](#4-分类管理接口)
  - [4.1 获取待分类图片列表](#41-获取待分类图片列表)
  - [4.2 提交分类结果](#42-提交分类结果)

---

## 接口基础信息

```json
{
  "code": "200",           // 状态码，成功为"200"，失败为其他
  "message": "操作成功",    // 提示信息
  "data": {}               // 返回数据，根据具体接口不同
}
```

### 状态码说明

| 状态码 | 说明 |
|--------|------|
| 200 | 请求成功 |
| 400 | 请求参数错误 |
| 401 | 未授权 |
| 403 | 禁止访问 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

---

## 1. 任务管理接口

### 1.1 获取任务列表

**接口描述：** 查询任务列表，支持分页和搜索

**请求方式：** `POST`

**请求路径：** `/api/docflow/task/list`

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| queryCondition | Object | 否 | 查询条件 |
| queryCondition.taskName | String | 否 | 任务名称（模糊搜索） |
| pageSize | Number | 是 | 每页条数 |
| currentPage | Number | 是 | 当前页码（从1开始） |

**请求示例：**

```json
{
  "queryCondition": {
    "taskName": "发票解析"
  },
  "pageSize": 10,
  "currentPage": 1
}
```

**响应参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| data | Object | 返回数据 |
| data.list | Array | 任务列表 |
| data.list[].id | Number/String | 任务ID |
| data.list[].taskName | String | 任务名称 |
| data.list[].status | String | 任务状态：待分类、解析中、已完成、失败 |
| data.list[].documentType | String | 单据类型编码 |
| data.list[].documentTypeName | String | 单据类型名称 |
| data.list[].fileCount | Number | 文件总数 |
| data.list[].classifiedCount | Number | 已分类数量 |
| data.list[].createTime | String | 创建时间，格式：YYYY-MM-DD HH:mm:ss |
| data.list[].updateTime | String | 更新时间，格式：YYYY-MM-DD HH:mm:ss |
| data.list[].errorMessage | String | 错误信息（失败时返回） |
| data.total | String | 总记录数 |

**响应示例：**

```json
{
  "code": "200",
  "message": "查询成功",
  "data": {
    "list": [
      {
        "id": 1,
        "taskName": "发票解析任务001",
        "status": "解析中",
        "documentType": "vat",
        "documentTypeName": "增值税发票",
        "fileCount": 15,
        "classifiedCount": 8,
        "createTime": "2025-10-18 10:30:00",
        "updateTime": "2025-10-20 09:15:00"
      },
      {
        "id": 2,
        "taskName": "合同解析任务002",
        "status": "已完成",
        "documentType": "financial_statement",
        "documentTypeName": "流水",
        "fileCount": 20,
        "classifiedCount": 20,
        "createTime": "2025-10-15 14:20:00",
        "updateTime": "2025-10-19 16:45:00"
      },
      {
        "id": 3,
        "taskName": "订单解析任务003",
        "status": "失败",
        "documentType": "order",
        "documentTypeName": "订单",
        "fileCount": 8,
        "classifiedCount": 0,
        "createTime": "2025-10-19 15:30:00",
        "updateTime": "2025-10-19 16:00:00",
        "errorMessage": "文件格式不支持"
      }
    ],
    "total": "22"
  }
}
```

---

### 1.2 创建任务

**接口描述：** 创建新的文档解析任务

**请求方式：** `POST`

**请求路径：** `/api/docflow/task/create`

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| taskName | String | 是 | 任务名称 |
| documentType | String | 是 | 单据类型编码 |
| fileIds | Object | 是 | 文件ID字典，key为单据类型，value为文件ID数组 |

**请求示例：**

```json
{
  "taskName": "发票解析任务001",
  "documentType": "vat",
  "fileIds": {
    "vat": ["file_id_001", "file_id_002", "file_id_003"],
    "financial_statement": ["file_id_004", "file_id_005"]
  }
}
```

**响应参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| data | Object | 返回数据 |
| data.taskId | Number/String | 创建的任务ID |

**响应示例：**

```json
{
  "code": "200",
  "message": "任务创建成功",
  "data": {
    "taskId": 123
  }
}
```

---

### 1.3 删除任务

**接口描述：** 删除指定任务

**请求方式：** `DELETE`

**请求路径：** `/api/docflow/task/{taskId}`

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| taskId | Number/String | 是 | 任务ID |

**响应示例：**

```json
{
  "code": "200",
  "message": "删除成功",
  "data": null
}
```

---

## 2. 单据类型接口

### 2.1 获取单据类型列表

**接口描述：** 获取系统支持的所有单据类型

**请求方式：** `POST`

**请求路径：** `/api/docflow/document/type/list`

**请求参数：** 无

**响应参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| data | Array | 单据类型列表 |
| data[].label | String | 单据类型名称 |
| data[].value | String | 单据类型编码 |

**响应示例：**

```json
{
  "code": "200",
  "message": "查询成功",
  "data": [
    {
      "label": "流水",
      "value": "financial_statement"
    },
    {
      "label": "增值税发票",
      "value": "vat"
    },
    {
      "label": "提单",
      "value": "bill_of_lading"
    },
    {
      "label": "订单",
      "value": "order"
    }
  ]
}
```

---

## 3. 文件管理接口

### 3.1 上传文件

**接口描述：** 上传单个文件到服务器

**请求方式：** `POST`

**请求路径：** `/api/docflow/file/upload`

**请求头：**

```
Content-Type: multipart/form-data
```

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| file | File | 是 | 文件对象 |
| documentType | String | 是 | 单据类型编码 |

**请求说明：**

- 支持的文件格式：pdf、png、jpg、jpeg
- 文件大小限制：单个文件最大 100MB
- 超时时间：300秒（5分钟）

**响应参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| data | Object | 返回数据 |
| data.fileId | String | 文件ID（唯一标识） |
| data.fileName | String | 文件名称 |
| data.filePath | String | 文件路径（可选） |
| data.fileSize | Number | 文件大小（字节） |
| data.uploadTime | String | 上传时间 |

**响应示例：**

```json
{
  "code": "200",
  "message": "上传成功",
  "data": {
    "fileId": "file_20251025_001",
    "fileName": "invoice_001.pdf",
    "filePath": "/uploads/2025/10/25/file_20251025_001.pdf",
    "fileSize": 1048576,
    "uploadTime": "2025-10-25 14:30:00"
  }
}
```

**错误响应示例：**

```json
{
  "code": "400",
  "message": "文件格式不支持，仅支持pdf、png、jpg、jpeg格式",
  "data": null
}
```

---

## 4. 分类管理接口

### 4.1 获取待分类图片列表

**接口描述：** 获取任务中需要分类的图片列表

**请求方式：** `GET`

**请求路径：** `/api/docflow/task/classify`

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| taskId | Number/String | 是 | 任务ID（Query参数） |

**响应参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| data | Array | 图片列表 |
| data[].id | String | 图片ID |
| data[].url | String | 图片URL地址 |
| data[].group | Number/null | 分组ID，null表示未分组 |
| data[].documentType | String | 单据类型 |

**响应示例：**

```json
{
  "code": "200",
  "message": "查询成功",
  "data": [
        {
          documentName: "增值税发票",
          documentType: "vat",
          imagesList: Array.from({ length: 60 }, (_, i) => ({
            id: `vat-${i + 1}`,
            url: `https://picsum.photos/seed/vat-${i + 1}/200/350`,
          })),
        },
        {
          documentName: "流水",
          documentType: "financial_statement",
          imagesList: Array.from({ length: 45 }, (_, i) => ({
            id: `financial-${i + 1}`,
            url: `https://picsum.photos/seed/financial-${i + 1}/200/350`,
          })),
        },
        {
          documentName: "提货单",
          documentType: "bill_of_lading",
          imagesList: Array.from({ length: 30 }, (_, i) => ({
            id: `bill-${i + 1}`,
            url: `https://picsum.photos/seed/bill-${i + 1}/200/350`,
          })),
        },
        {
          documentName: "合同",
          documentType: "contract",
          imagesList: Array.from({ length: 20 }, (_, i) => ({
            id: `contract-${i + 1}`,
            url: `https://picsum.photos/seed/contract-${i + 1}/200/350`,
          })),
        },
      ]
}
```

---

### 4.2 提交分类结果

**接口描述：** 提交用户对图片的分类结果

**请求方式：** `POST`

**请求路径：** `/api/docflow/task/classify/submit`

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| taskId | Number/String | 是 | 任务ID |
| data | Array | 是 | 分类数据列表 |
| data[].documentType | String | 是 | 单据类型编码 |
| data[].documentName | String | 是 | 单据类型名称 |
| data[].groups | Array | 是 | 分组列表 |
| data[].groups[].groupId | Number | 是 | 组ID |
| data[].groups[].images | Array | 是 | 图片列表 |
| data[].groups[].images[].id | String | 是 | 图片ID |
| data[].groups[].images[].url | String | 是 | 图片URL |

**请求示例：**

```json
{
  "taskId": 123,
  "data": [
    {
      "documentType": "vat",
      "documentName": "增值税发票",
      "groups": [
        {
          "groupId": 1,
          "images": [
            {
              "id": "vat-1",
              "url": "https://your-domain.com/images/vat-1.jpg"
            },
            {
              "id": "vat-2",
              "url": "https://your-domain.com/images/vat-2.jpg"
            }
          ]
        },
        {
          "groupId": 2,
          "images": [
            {
              "id": "vat-3",
              "url": "https://your-domain.com/images/vat-3.jpg"
            }
          ]
        }
      ]
    },
    {
      "documentType": "financial_statement",
      "documentName": "流水",
      "groups": [
        {
          "groupId": 1,
          "images": [
            {
              "id": "financial-1",
              "url": "https://your-domain.com/images/financial-1.jpg"
            },
            {
              "id": "financial-2",
              "url": "https://your-domain.com/images/financial-2.jpg"
            },
            {
              "id": "financial-3",
              "url": "https://your-domain.com/images/financial-3.jpg"
            }
          ]
        }
      ]
    }
  ]
}
```

**响应示例：**

```json
{
  "code": "200",
  "message": "提交成功",
  "data": null
}
```

---

## 附录

### A. 单据类型编码对照表

| 编码 | 名称 | 说明 |
|------|------|------|
| vat | 增值税发票 | 增值税专用发票/普通发票 |
| financial_statement | 流水 | 财务流水 |
| bill_of_lading | 提单 | 提货单/提单 |
| order | 订单 | 订单类文档 |
| contract | 合同 | 合同类文档 |

### B. 任务状态说明

| 状态 | 说明 |
|------|------|
| 待分类 | 任务已创建，等待用户分类 |
| 解析中 | 正在进行文档解析 |
| 已完成 | 任务已完成 |
| 失败 | 任务执行失败 |

### C. 前端文件结构处理说明

**上传流程：**

1. 用户选择文件后，前端调用 `/api/docflow/file/upload` 接口上传文件
2. 后端返回 `fileId`
3. 前端收集所有 `fileId`，按单据类型分组
4. 提交任务时，将 `fileIds` 对象发送给后端

**示例：**

```javascript
// 上传完成后得到的数据结构
const fileIds = {
  "vat": ["file_id_001", "file_id_002"],
  "financial_statement": ["file_id_003"]
};

// 创建任务时提交
await createTask({
  taskName: "任务名称",
  documentType: "vat",
  fileIds: fileIds
});
```

### D. 注意事项

1. **文件上传**
   
   - 文件上传需要设置较长的超时时间（建议5分钟）
   - 上传失败的文件需要提示用户重新上传
2. **状态码**
   
   - 成功状态码为字符串 `"200"`，不是数字 200
   - 前端通过 `response.code === "200"` 判断成功
3. **图片URL**
   
   - 返回完整的可访问URL
   - 支持HTTPS协议
   - 建议使用CDN加速
4. **错误处理**
   
   - 所有接口需要返回明确的错误信息
   - message 字段需要包含用户友好的提示文本
   - 失败时 data 可以为 null 或包含错误详情

---

## 更新日志

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v1.0 | 2025-10-25 | 初始版本，包含所有基础接口 |

---

```

```
